pipeline {
  agent any
  stages {
    stage('Docker Build') {
steps {
         script {
          switch(BRANCH) {
            case "main": 
              echo 'main sanjaya1'
              break
            case "test": 
              echo 'test2'
              break
          }
        }
      }
    }

    
}
}
